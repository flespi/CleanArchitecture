// Keep this file as is, without changes.
// The values are taken from the `settings.json` asset in production mode.
// This allows the configuration to be loaded at run time.

export const settings = { }
